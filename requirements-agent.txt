# Agent VM Dependencies
# Install with: pip install -r requirements-agent.txt

# Communication
requests==2.31.0
redis==5.0.1

# Configuration
python-dotenv==1.0.0
APScheduler==3.10.4

# Your existing dependencies (should already be installed)
numpy>=1.20.0
scipy>=1.7.0
psutil>=5.8.0
pyyaml>=6.0
